个人财务记账系统 - 详细设计方案
一、项目概述
1.1 项目定位
一个面向大学生和年轻群体的轻量级个人财务管理系统,帮助用户记录日常收支、分析消费习惯、制定预算计划,培养良好的理财意识。
1.2 核心价值

记账便捷:快速记录每笔收支
数据可视:图表展示消费结构
预算管理:设置预算并追踪完成情况
习惯分析:发现消费模式和优化空间

1.3 技术亮点

📊 丰富的数据可视化(ECharts多维度图表)
💰 复杂的金额计算和统计逻辑
📱 响应式设计,支持移动端
🔐 完整的用户认证和数据隔离
📈 预算预警和智能提醒


二、功能模块详细设计
2.1 用户认证模块
功能点:

用户注册(邮箱/手机号)
用户登录(JWT Token认证)
密码找回
个人信息编辑

技术实现:

前端表单验证(邮箱格式、密码强度)
后端JWT生成和验证
密码加密存储(BCrypt)
Token刷新机制


2.2 记账核心模块
2.2.1 快速记账
界面设计:
+---------------------------+
|    收入  |  支出  |  转账  |  (Tab切换)
+---------------------------+
| 金额: ¥ [_______]         |
| 分类: [餐饮 ▼]            |
| 账户: [微信 ▼]            |
| 时间: [2024-12-12 ▼]     |
| 备注: [_____________]     |
|                           |
|      [取消]  [保存]       |
+---------------------------+
分类体系:
支出分类:

🍔 餐饮(早餐/午餐/晚餐/夜宵/零食/饮料)
🚇 交通(地铁/公交/打车/加油)
🎮 娱乐(电影/游戏/KTV/旅游)
🛒 购物(衣服/鞋子/日用品/数码)
📚 学习(书籍/课程/文具)
🏥 医疗(看病/买药/体检)
🏠 居住(房租/水电/物业)
📱 通讯(话费/宽带)
👥 社交(礼物/聚餐/红包)
💄 美容(护肤/美发/化妆品)
🏃 运动(健身/装备)
🐕 宠物(食品/医疗/用品)
📦 其他

收入分类:

💰 工资
🎁 奖金
💸 兼职
📈 投资收益
🧧 红包
💳 退款
📦 其他

账户类型:

💳 现金
🏦 银行卡(支持多张)
💚 微信
💙 支付宝
💛 饭卡
📦 其他

数据库设计:
sql-- 账单表
CREATE TABLE transactions (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    user_id BIGINT NOT NULL,
    type ENUM('income', 'expense', 'transfer') NOT NULL,
    amount DECIMAL(10,2) NOT NULL,
    category_id INT NOT NULL,
    account_id INT NOT NULL,
    to_account_id INT NULL, -- 转账目标账户
    transaction_date DATETIME NOT NULL,
    remark VARCHAR(200),
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    INDEX idx_user_date (user_id, transaction_date),
    INDEX idx_category (category_id)
);

-- 分类表
CREATE TABLE categories (
    id INT PRIMARY KEY AUTO_INCREMENT,
    name VARCHAR(50) NOT NULL,
    type ENUM('income', 'expense') NOT NULL,
    icon VARCHAR(50),
    parent_id INT NULL, -- 支持二级分类
    sort_order INT DEFAULT 0
);

-- 账户表
CREATE TABLE accounts (
    id INT PRIMARY KEY AUTO_INCREMENT,
    user_id BIGINT NOT NULL,
    name VARCHAR(50) NOT NULL,
    type ENUM('cash', 'bank', 'wechat', 'alipay', 'other') NOT NULL,
    balance DECIMAL(10,2) DEFAULT 0,
    initial_balance DECIMAL(10,2) DEFAULT 0, -- 初始余额
    icon VARCHAR(50),
    is_default BOOLEAN DEFAULT FALSE,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);
```

---

### 2.3 账单管理模块

#### 2.3.1 账单列表
**功能:**
- 按日期分组展示
- 显示每日收支汇总
- 支持下拉刷新和上拉加载
- 快速编辑/删除

**界面布局:**
```
+---------------------------+
| [筛选] [搜索]   [本月▼]   |
+---------------------------+
| 2024年12月12日  收¥200 支¥156
+---------------------------+
| 🍔 午餐          -¥35.00  |
|    麦当劳套餐    12:30    |
+---------------------------+
| 🚇 地铁          -¥6.00   |
|    回家          18:20    |
+---------------------------+
| 2024年12月11日  收¥0 支¥89
+---------------------------+
| ...                       |
+---------------------------+
2.3.2 筛选功能

时间范围筛选(今天/本周/本月/自定义)
分类筛选(多选)
账户筛选
收支类型筛选
金额区间筛选
关键词搜索(备注)

筛选器API设计:
javascriptGET /api/transactions?
    userId=123
    &startDate=2024-12-01
    &endDate=2024-12-31
    &type=expense
    &categoryIds=1,2,3
    &accountIds=1,2
    &minAmount=0
    &maxAmount=1000
    &keyword=午餐
    &page=1
    &pageSize=20
```

---

### 2.4 数据统计模块

#### 2.4.1 首页仪表盘

**关键指标:**
```
+---------------------------+
|    本月收支概览            |
+---------------------------+
|  收入: ¥5,000            |
|  支出: ¥3,245   剩余65%   |
|  结余: ¥1,755   ↑ 12%    |
+---------------------------+
|    [本周] [本月] [本年]   |
+---------------------------+
支出趋势图(折线图):

X轴:日期
Y轴:金额
展示最近7天/30天的每日支出趋势

分类占比图(饼图):

展示本月各分类支出占比
点击可查看分类详情

收支对比图(柱状图):

对比最近6个月的收支情况

2.4.2 详细统计页面
时间维度:

日统计
周统计
月统计
年统计
自定义时间段

统计维度:

按分类统计

各分类金额和占比
同比/环比增长


按账户统计

各账户收支情况
账户余额变化


按时间统计

每日/每周/每月趋势
工作日vs周末消费对比


消费排行

单笔金额TOP10
消费频次最高的分类



核心SQL查询示例:
sql-- 按分类统计本月支出
SELECT 
    c.name as category_name,
    SUM(t.amount) as total_amount,
    COUNT(*) as count,
    ROUND(SUM(t.amount) / (
        SELECT SUM(amount) 
        FROM transactions 
        WHERE user_id = ? 
        AND type = 'expense'
        AND MONTH(transaction_date) = MONTH(CURRENT_DATE)
    ) * 100, 2) as percentage
FROM transactions t
JOIN categories c ON t.category_id = c.id
WHERE t.user_id = ?
AND t.type = 'expense'
AND MONTH(t.transaction_date) = MONTH(CURRENT_DATE)
GROUP BY t.category_id, c.name
ORDER BY total_amount DESC;

2.5 预算管理模块
2.5.1 预算设置
预算类型:

总预算(每月总支出限额)
分类预算(餐饮、交通等单独设置)

预算周期:

月度预算(最常用)
年度预算

数据库设计:
sqlCREATE TABLE budgets (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    user_id BIGINT NOT NULL,
    category_id INT NULL, -- NULL表示总预算
    amount DECIMAL(10,2) NOT NULL,
    period_type ENUM('monthly', 'yearly') NOT NULL,
    start_date DATE NOT NULL,
    end_date DATE,
    alert_threshold INT DEFAULT 80, -- 预警阈值(百分比)
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);
```

#### 2.5.2 预算监控

**预算卡片显示:**
```
+---------------------------+
| 🍔 餐饮预算               |
+---------------------------+
| 已用: ¥856 / ¥1000       |
| [==========85%========>  ]|
| 剩余: ¥144 (还有20天)     |
| ⚠️ 接近预算限额!          |
+---------------------------+
```

**预警机制:**
- 达到80%时黄色预警
- 达到100%时红色警告
- 超出预算时特别提示

**预算分析:**
- 预算完成率趋势
- 历史预算执行情况
- 预算vs实际支出对比

---

### 2.6 账户管理模块

#### 2.6.1 账户总览
```
+---------------------------+
|    总资产: ¥12,450        |
+---------------------------+
| 💳 现金          ¥350    |
| 🏦 工商银行      ¥8,500  |
| 💚 微信          ¥2,100  |
| 💙 支付宝        ¥1,500  |
+---------------------------+
|   [+ 添加账户]            |
+---------------------------+
2.6.2 账户详情

账户余额变化曲线
该账户的所有交易记录
收支统计

2.6.3 账户间转账

选择转出账户和转入账户
记录转账金额
自动调整两个账户余额

转账逻辑:
python# FastAPI + SQLAlchemy 实现
async def transfer(from_account_id: int, to_account_id: int, amount: float, db: Session):
    # 开启事务
    try:
        # 1. 扣减转出账户
        from_account = db.query(Account).filter(Account.id == from_account_id).first()
        if from_account.balance < amount:
            raise ValueError("余额不足")
        from_account.balance -= amount

        # 2. 增加转入账户
        to_account = db.query(Account).filter(Account.id == to_account_id).first()
        to_account.balance += amount

        # 3. 记录转账交易
        transfer_transaction = Transaction(
            user_id=from_account.user_id,
            type="transfer",
            amount=amount,
            account_id=from_account_id,
            to_account_id=to_account_id,
            transaction_date=datetime.now(),
            remark=f"转账至 {to_account.name}"
        )
        db.add(transfer_transaction)

        db.commit()
    except Exception as e:
        db.rollback()
        raise e

2.7 报表导出模块
2.7.1 Excel导出
导出内容:

账单明细(所有字段)
分类统计汇总
月度报表

技术实现(后端):
python# FastAPI + openpyxl 实现
from fastapi import Response
from openpyxl import Workbook
from io import BytesIO

@app.get("/api/statistics/export/excel")
async def export_excel(current_user: User = Depends(get_current_user)):
    workbook = Workbook()

    # Sheet1: 账单明细
    sheet1 = workbook.active
    sheet1.title = "账单明细"

    # 创建表头
    headers = ["日期", "类型", "分类", "金额", "账户", "备注"]
    for col, header in enumerate(headers, 1):
        sheet1.cell(row=1, column=col, value=header)

    # 填充数据
    transactions = get_user_transactions(current_user.id)
    for row, transaction in enumerate(transactions, 2):
        sheet1.cell(row=row, column=1, value=transaction.transaction_date.strftime("%Y-%m-%d"))
        sheet1.cell(row=row, column=2, value=transaction.type)
        sheet1.cell(row=row, column=3, value=transaction.category.name)
        sheet1.cell(row=row, column=4, value=float(transaction.amount))
        sheet1.cell(row=row, column=5, value=transaction.account.name)
        sheet1.cell(row=row, column=6, value=transaction.remark or "")

    # Sheet2: 统计汇总
    sheet2 = workbook.create_sheet("统计汇总")
    # ... 统计数据填充逻辑

    # 保存到字节流
    output = BytesIO()
    workbook.save(output)
    output.seek(0)

    filename = f"账单_{datetime.now().strftime('%Y%m%d')}.xlsx"
    headers = {
        "Content-Disposition": f"attachment; filename={filename}",
        "Content-Type": "application/vnd.openxmlformats-officedocument.spreadsheetml.sheet"
    }

    return Response(
        content=output.getvalue(),
        headers=headers,
        media_type="application/vnd.openxmlformats-officedocument.spreadsheetml.sheet"
    )
2.7.2 PDF报表
月度报表内容:

封面(月份、用户名)
收支总览
支出饼图
趋势折线图
分类明细表


2.8 智能提醒模块
2.8.1 提醒类型

记账提醒

每晚9点提醒今天是否记账
检测到今天无记录时触发


预算预警

达到预算阈值时推送
每天检查一次


账单周期提醒

固定日期提醒(如每月1日房租)
需要用户预设


数据分析报告

每月1日推送上月总结
每周一推送上周分析



数据库设计:
sqlCREATE TABLE reminders (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    user_id BIGINT NOT NULL,
    type ENUM('daily', 'budget', 'recurring', 'report') NOT NULL,
    title VARCHAR(100),
    content TEXT,
    remind_time TIME, -- 提醒时间
    remind_day INT, -- 每月第几天(循环提醒)
    category_id INT NULL, -- 关联分类(预算提醒)
    is_enabled BOOLEAN DEFAULT TRUE,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);
```

---

## 三、技术架构设计

### 3.1 前端技术栈

**框架选型:**
```
Vue 3 + TypeScript
├── 状态管理: Pinia
├── 路由: Vue Router 4
├── UI组件库: Element Plus (PC端) / Vant (移动端)
├── 图表库: ECharts 5
├── HTTP客户端: Axios
├── 日期处理: Day.js
├── 表单验证: VeeValidate
├── Excel导出: SheetJS (xlsx)
└── 构建工具: Vite
```

**项目结构:**
```
src/
├── assets/          # 静态资源
│   ├── images/
│   └── styles/
├── components/      # 通用组件
│   ├── common/
│   │   ├── CategoryIcon.vue      # 分类图标
│   │   ├── AmountInput.vue       # 金额输入
│   │   └── DatePicker.vue        # 日期选择
│   ├── charts/
│   │   ├── PieChart.vue          # 饼图
│   │   ├── LineChart.vue         # 折线图
│   │   └── BarChart.vue          # 柱状图
│   └── layout/
│       ├── Header.vue
│       ├── Sidebar.vue
│       └── BottomNav.vue         # 移动端底部导航
├── views/           # 页面组件
│   ├── auth/
│   │   ├── Login.vue
│   │   └── Register.vue
│   ├── home/
│   │   └── Dashboard.vue         # 首页仪表盘
│   ├── transaction/
│   │   ├── TransactionList.vue   # 账单列表
│   │   ├── AddTransaction.vue    # 添加账单
│   │   └── TransactionDetail.vue # 账单详情
│   ├── statistics/
│   │   ├── Overview.vue          # 统计总览
│   │   └── CategoryAnalysis.vue  # 分类分析
│   ├── budget/
│   │   ├── BudgetList.vue        # 预算列表
│   │   └── BudgetSetting.vue     # 预算设置
│   ├── account/
│   │   ├── AccountList.vue       # 账户列表
│   │   └── AccountDetail.vue     # 账户详情
│   └── settings/
│       └── Settings.vue          # 设置页面
├── router/          # 路由配置
│   └── index.ts
├── store/           # 状态管理
│   ├── modules/
│   │   ├── user.ts              # 用户状态
│   │   ├── transaction.ts       # 交易状态
│   │   └── budget.ts            # 预算状态
│   └── index.ts
├── api/             # API接口
│   ├── auth.ts
│   ├── transaction.ts
│   ├── statistics.ts
│   ├── budget.ts
│   └── account.ts
├── utils/           # 工具函数
│   ├── request.ts               # Axios封装
│   ├── auth.ts                  # Token管理
│   ├── format.ts                # 格式化工具
│   └── validate.ts              # 验证工具
├── types/           # TypeScript类型
│   ├── transaction.ts
│   ├── budget.ts
│   └── user.ts
├── App.vue
└── main.ts
```

---

### 3.2 后端技术栈

**推荐方案: FastAPI**
```
FastAPI
├── 数据库: MySQL 8.0 + SQLAlchemy/SQLModel
├── 缓存: Redis
├── 认证: python-jose + passlib (JWT)
├── 文档: 自动生成 OpenAPI/Swagger
├── 工具:
│   ├── Pydantic (数据验证)
│   ├── Alembic (数据库迁移)
│   └── openpyxl (Excel导出)
└── 测试: pytest + httpx
```

**项目结构:**
```
app/
├── main.py                      # FastAPI 应用入口
├── config/                      # 配置文件
│   ├── __init__.py
│   ├── database.py              # 数据库配置
│   ├── redis.py                 # Redis配置
│   └── settings.py              # 应用设置
├── api/                         # API 路由
│   ├── __init__.py
│   ├── auth.py                  # 认证相关接口
│   ├── transactions.py          # 交易记录接口
│   ├── statistics.py            # 统计分析接口
│   ├── budgets.py               # 预算管理接口
│   └── accounts.py              # 账户管理接口
├── models/                      # SQLAlchemy 数据库模型
│   ├── __init__.py
│   ├── user.py
│   ├── transaction.py
│   ├── category.py
│   ├── account.py
│   └── budget.py
├── schemas/                     # Pydantic 数据模型
│   ├── __init__.py
│   ├── user.py
│   ├── transaction.py
│   ├── category.py
│   ├── account.py
│   └── budget.py
├── services/                    # 业务逻辑层
│   ├── __init__.py
│   ├── auth_service.py
│   ├── transaction_service.py
│   ├── statistics_service.py
│   ├── budget_service.py
│   └── account_service.py
├── core/                        # 核心功能
│   ├── __init__.py
│   ├── security.py              # 安全相关
│   ├── dependencies.py          # 依赖注入
│   └── exceptions.py            # 异常处理
├── utils/                       # 工具函数
│   ├── __init__.py
│   ├── jwt_utils.py             # JWT工具
│   ├── date_utils.py            # 日期工具
│   └── excel_utils.py           # Excel工具
└── tests/                       # 测试文件
    ├── __init__.py
    ├── test_auth.py
    ├── test_transactions.py
    └── conftest.py
```

3.3 数据库设计完整版
sql-- 用户表
CREATE TABLE users (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    username VARCHAR(50) UNIQUE NOT NULL,
    email VARCHAR(100) UNIQUE NOT NULL,
    password VARCHAR(255) NOT NULL,
    avatar VARCHAR(255),
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP
);

-- 分类表
CREATE TABLE categories (
    id INT PRIMARY KEY AUTO_INCREMENT,
    name VARCHAR(50) NOT NULL,
    type ENUM('income', 'expense') NOT NULL,
    icon VARCHAR(50),
    color VARCHAR(20),
    parent_id INT NULL,
    sort_order INT DEFAULT 0,
    is_system BOOLEAN DEFAULT FALSE, -- 是否系统预设分类
    INDEX idx_type (type)
);

-- 账户表
CREATE TABLE accounts (
    id INT PRIMARY KEY AUTO_INCREMENT,
    user_id BIGINT NOT NULL,
    name VARCHAR(50) NOT NULL,
    type ENUM('cash', 'bank', 'wechat', 'alipay', 'meal_card', 'other') NOT NULL,
    balance DECIMAL(10,2) DEFAULT 0,
    initial_balance DECIMAL(10,2) DEFAULT 0,
    icon VARCHAR(50),
    color VARCHAR(20),
    is_default BOOLEAN DEFAULT FALSE,
    is_enabled BOOLEAN DEFAULT TRUE,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    FOREIGN KEY (user_id) REFERENCES users(id) ON DELETE CASCADE,
    INDEX idx_user (user_id)
);

-- 交易记录表
CREATE TABLE transactions (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    user_id BIGINT NOT NULL,
    type ENUM('income', 'expense', 'transfer') NOT NULL,
    amount DECIMAL(10,2) NOT NULL,
    category_id INT NOT NULL,
    account_id INT NOT NULL,
    to_account_id INT NULL,
    transaction_date DATETIME NOT NULL,
    remark VARCHAR(200),
    images JSON, -- 存储图片URL数组
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    FOREIGN KEY (user_id) REFERENCES users(id) ON DELETE CASCADE,
    FOREIGN KEY (category_id) REFERENCES categories(id),
    FOREIGN KEY (account_id) REFERENCES accounts(id),
    INDEX idx_user_date (user_id, transaction_date),
    INDEX idx_category (category_id),
    INDEX idx_type (type)
);

-- 预算表
CREATE TABLE budgets (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    user_id BIGINT NOT NULL,
    category_id INT NULL,
    amount DECIMAL(10,2) NOT NULL,
    period_type ENUM('monthly', 'yearly') NOT NULL,
    year INT NOT NULL,
    month INT NULL, -- 月度预算时使用
    alert_threshold INT DEFAULT 80,
    is_enabled BOOLEAN DEFAULT TRUE,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    FOREIGN KEY (user_id) REFERENCES users(id) ON DELETE CASCADE,
    FOREIGN KEY (category_id) REFERENCES categories(id),
    UNIQUE KEY uk_budget (user_id, category_id, year, month),
    INDEX idx_user_period (user_id, year, month)
);

-- 提醒表
CREATE TABLE reminders (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    user_id BIGINT NOT NULL,
    type ENUM('daily', 'budget', 'recurring', 'report') NOT NULL,
    title VARCHAR(100),
    content TEXT,
    remind_time TIME,
    remind_day INT, -- 每月第几天
    category_id INT NULL,
    amount DECIMAL(10,2) NULL, -- 固定金额提醒
    is_enabled BOOLEAN DEFAULT TRUE,
    last_reminded_at DATETIME,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    FOREIGN KEY (user_id) REFERENCES users(id) ON DELETE CASCADE,
    INDEX idx_user_type (user_id, type)
);

-- 统计缓存表(可选,用于加速查询)
CREATE TABLE statistics_cache (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    user_id BIGINT NOT NULL,
    stat_type VARCHAR(50) NOT NULL, -- 'monthly_summary', 'category_summary'等
    period VARCHAR(20) NOT NULL, -- '2024-12', '2024'等
    data JSON NOT NULL,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    UNIQUE KEY uk_stat (user_id, stat_type, period),
    INDEX idx_user (user_id)
);
```

---

### 3.4 核心API接口设计

#### 3.4.1 认证相关
```
POST   /api/auth/register        # 用户注册
POST   /api/auth/login           # 用户登录
POST   /api/auth/logout          # 用户登出
POST   /api/auth/refresh-token   # 刷新Token
POST   /api/auth/forgot-password # 忘记密码
```

#### 3.4.2 交易相关
```
GET    /api/transactions                 # 获取交易列表(支持筛选)
GET    /api/transactions/:id             # 获取交易详情
POST   /api/transactions                 # 添加交易
PUT    /api/transactions/:id             # 更新交易
DELETE /api/transactions/:id             # 删除交易
POST   /api/transactions/transfer        # 账户转账
GET    /api/transactions/summary         # 获取汇总信息
添加交易接口示例:
json// Request
POST /api/transactions
{
  "type": "expense",
  "amount": 35.5,
  "categoryId": 1,
  "accountId": 2,
  "transactionDate": "2024-12-12 12:30:00",
  "remark": "麦当劳午餐"
}

// Response
{
  "code": 200,
  "message": "success",
  "data": {
    "id": 12345,
    "type": "expense",
    "amount": 35.5,
    "category": {
      "id": 1,
      "name": "餐饮",
      "icon": "🍔"
    },
    "account": {
      "id": 2,
      "name": "微信",
      "balance": 1964.5
    },
    "transactionDate": "2024-12-12 12:30:00",
    "remark": "麦当劳午餐",
    "createdAt": "2024-12-12 12:31:00"
  }
}
```

#### 3.4.3 统计相关
```
GET /api/statistics/overview          # 首页概览数据
GET /api/statistics/trend             # 趋势数据
GET /api/statistics/category          # 分类统计
GET /api/statistics/account           # 账户统计
GET /api/statistics/monthly-report    # 月度报表
GET /api/statistics/export/excel      # 导出Excel
GET /api/statistics/export/pdf        # 导出PDF
首页概览接口示例:
json// Request
GET /api/statistics/overview?period=2024-12

// Response
{
  "code": 200,
  "data": {
    "period": "2024-12",
    "income": {
      "amount": 5000,
      "count": 2,
      "change": 12.5  // 环比增长%
    },
    "expense": {
      "amount": 3245,
      "count": 156,
      "change": -5.2
    },
    "balance": 1755,
    "topCategories": [
      {
        "categoryId": 1,
        "categoryName": "餐饮",